name: "Test Pull Request"

on: push

jobs:
  build:
   runs-on: ubuntu-latest
   steps:
   - uses: actions/checkout@v3
   - uses: Sanj57/Auto-Merge@master
     with:
      github-token: ${{ secrets.GITHUB_TOKEN }}
      number: 1
      method: merge
 
